<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>shoのページ</title>
	</head>
	<body>
		<slot />
	</body>
</html>

<style>
html,
body {
		margin: 0;
		width: 100%;
		height: 100%;
}

main, .markdown-body {
	max-width: 720px;
	margin-left: auto;
	margin-right: auto;
	padding: 2.5rem 1.2rem 3rem 1.2rem;
	background: #fff;
	border-radius: 1.5rem;
	box-shadow: 0 4px 32px 0 rgba(0,0,0,0.07);
	font-family: 'Segoe UI', 'Hiragino Sans', 'Meiryo', sans-serif;
}

/* Markdown headings */
h1, h2, h3, h4, h5, h6 {
	font-family: inherit;
	font-weight: 800;
	color: #334155;
	margin-top: 2.2em;
	margin-bottom: 0.7em;
	line-height: 1.2;
}
h1 { font-size: 2.1rem; border-bottom: 2px solid #a5b4fc; padding-bottom: 0.3em; }
h2 { font-size: 1.5rem; border-left: 5px solid #a5b4fc; padding-left: 0.5em; }
h3 { font-size: 1.2rem; color: #0e7490; }

/* Markdown lists */
ul, ol {
	padding-left: 1.5em;
	margin-bottom: 1.2em;
}
li {
	margin-bottom: 0.3em;
	line-height: 1.7;
}

/* Markdown blockquote */
blockquote {
	border-left: 4px solid #a5b4fc;
	background: #f0fdfa;
	color: #334155;
	margin: 1.5em 0;
	padding: 1em 1.5em;
	border-radius: 0.7em;
	font-style: italic;
}

/* Markdown links */
a {
	color: #2563eb;
	text-decoration: underline;
	transition: color 0.2s;
}
a:hover {
	color: #0e7490;
}

/* Markdown hr */
hr {
	border: none;
	border-top: 2px dashed #a5b4fc;
	margin: 2.5em 0;
}

/* Markdown table */
table {
	border-collapse: collapse;
	width: 100%;
	margin: 2em 0;
}
th, td {
	border: 1px solid #e0e7ff;
	padding: 0.6em 1em;
}
th {
	background: #f0fdfa;
	color: #334155;
}

/* Markdown code */
code, pre {
	font-family: 'Fira Mono', 'Consolas', monospace;
	background: #f1f5f9;
	color: #334155;
	border-radius: 0.3em;
	padding: 0.2em 0.4em;
}
pre {
	padding: 1em;
	overflow-x: auto;
}
</style>
